Pour trouver ce fichier c'était plus sensible deja. 
On a chercher flag01, sans resultat. On a donc chercher les fichiers qui ont a l'interieur ecrit flag01.

find / -type f -name "flag01" -exec grep -l "flag01" {} \; 2>/dev/null

2 fichiers sont trouvé :
/etc/group
/etc/passwd

Le /etc/passwd etait le bon, il contenant tout les flagXX mais le seul qui avait quelque chose de visible etait le flag01.

On recupere ce code et bien sur il est hashé, evidement john était le bon candidat. 

Le mdp : abcdefg.